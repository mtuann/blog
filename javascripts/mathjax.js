window.MathJax = {
  tex: {
    inlineMath: [["\\(", "\\)"]],
    displayMath: [["\\[", "\\]"]],
    processEscapes: true,
    processEnvironments: true
  },
  options: {
    ignoreHtmlClass: ".*|",
    processHtmlClass: "arithmatex"
  }
};

document.getElementById("pk-mode-toggle")?.addEventListener("click", () => {
    // Re-render math when switching themes if necessary, though Material handles this well usually.
})
