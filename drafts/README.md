# Drafts

This directory is intentionally **outside** `docs/` so unfinished notes never get published by accident.

Suggested use:

- Put reading queues, scratch translations, and incomplete article drafts here.
- Promote a note into `docs/` only when it is ready to be part of the public site.
- Keep one inbox file for ideas that are worth revisiting later.
